{% include 'header' %}

import ReactiveKit

extension {{ module_info.name }} {

  struct RouterOutput { }

  class Router: BaseRouter {

    private weak var viewController: UIViewController?
    private let modulesLoaders: ModulesLoaders

    init(viewController: UIViewController, modulesLoaders: ModulesLoaders) {
      self.viewController = viewController
      self.modulesLoaders = modulesLoaders
    }

    var output: RouterOutput {
      return RouterOutput()
    }
    
    func bind(to input: RouterInput) { }
  }

}